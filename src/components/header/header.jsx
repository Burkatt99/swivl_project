import React from "react";
import { HeaderContainer, HeaderTitle } from "./header.styles";

const Header = () => {
    return (
        <>
            <HeaderContainer>
                <HeaderTitle>SWIVL PROJECT</HeaderTitle>
            </HeaderContainer>
        </>
    );
};

export default Header;
